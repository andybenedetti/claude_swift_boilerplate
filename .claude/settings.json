{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | { read f; [[ \"$f\" == *.pbxproj ]] && { echo 'BLOCKED: Use the xcodeproj MCP server instead of editing pbxproj directly.' >&2; exit 2; } || exit 0; }"
          }
        ]
      }
    ]
  }
}
